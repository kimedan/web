[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Force legacy peer deps to fix ERESOLVE errors with lucide-react
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"
